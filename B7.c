/*
Write a C program to remove any negative sign in front of a number.
Input a value (negative):
 Expected Output:
Original value = -253
Absolute value = 253
*/

#include <stdio.h>

int main () 
{
  int a;
  printf("Provide a number negative or positive : ");
  scanf("%d", &a);
  if (x < 0) {
    printf("Absolute value = %d\n", -1*x);
  }
  else {
    printf("Absolute value = %d\n", x);
  }
  return 0;
}
